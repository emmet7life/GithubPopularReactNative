import {onThemeChange} from "./theme";
import {onRefreshPopular, onLoadMorePopular} from "./popular";

// 统一导出支持的action
export default {
    onThemeChange,
    onRefreshPopular,
    onLoadMorePopular,
}